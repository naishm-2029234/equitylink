import React from 'react';

function WelcomePage() { // Capitalized function name
    // const navigate = useNavigate();
    return (
        <h1>Homepage</h1>
    );
}

export default WelcomePage; // Capitalized export name
